{
  "name": "package.json",
  "version": "1.0.0",
  "description": "It is raw node project",
  "main": "main.js",
  "scripts": {
    "start": "cross-env NODE_ENV=staging nodemon main",
    "dev": "cross-env NODE_ENV=production nodemon main",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "apurbo howlader",
  "license": "ISC",
  "devDependencies": {
    "cross-env": "^6.0.3"
  }
}
